<div class="products">
  <h1>Productos</h1>

  {{#each products}}
    <div class="product-row"><h2>{{this.title}}</h2><p>Precio: ${{this.price}}</p></div>

  {{else}}
    <h2>No se encontraron productos :(</h2>
  {{/each}}
</div>

<a href="/" class="button">Agregar producto</a>